-- ============================================================
-- seed.sql — local development seed data
-- Only runs during `supabase db reset`, not in production.
-- ============================================================

-- Example: insert a test profile
-- (In practice, profiles are created automatically by the auth trigger.
--  This is here as a reference for seeding other tables.)

-- insert into public.profiles (id, username, full_name)
-- values
--   ('00000000-0000-0000-0000-000000000001', 'alice', 'Alice Example'),
--   ('00000000-0000-0000-0000-000000000002', 'bob',   'Bob Example');
